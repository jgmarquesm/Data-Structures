name: C CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  unit_tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: listar dirs
      run: cd resources/scripts/test/ && cat test_suite.sh
    - name: teste
      run: ./resources/scripts/test/test_suite.sh
